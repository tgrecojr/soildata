# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=uscrn
DB_USER=uscrn
DB_PASSWORD=uscrn_password

# PostgreSQL settings (for docker-compose)
POSTGRES_USER=uscrn
POSTGRES_PASSWORD=uscrn_password
POSTGRES_DB=uscrn

# Logging level
RUST_LOG=info,uscrn_ingest=debug
